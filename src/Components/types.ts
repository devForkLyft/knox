export type ButtonProps = {
  text: string;
  variant?: "text" | "outlined" | "contained";
  color?: string;
  backgroundColor?: string;
};

export type TitleProps = {
  title: string;
  iconSrc: string;
};